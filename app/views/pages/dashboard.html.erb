<div class="container-fluid">
  <% @eventos_por_mes.each do |mes, eventos_mensais| %>
    <h2><%= mes_br(mes) %></h2>

    <table class="table text-center">
      <thead>
        <tr>
          <th class="text-start" scope="col">Nome</th>
          <th scope="col">Cachê</th>
          <th scope="col">Covert</th>
          <th scope="col">Data</th>
        </tr>
      </thead>
      <tbody>
        <% eventos_mensais.each do |evento| %>
          <tr>
            <th class="text-start" scope="row"><%= evento.nome_evento %></th>
            <td>R$ <%= number_with_precision(evento.cache, precision: 2) %></td>
            <td>R$ <%= number_with_precision(evento.cover, precision: 2) %></td>
            <td><%= formatar_data_br(evento.data_inicio) %></td>
          </tr>
        <% end %>
        <tr>
          <td></td>
          <td></td>
          <td></td>
          <td></td>
        </tr>
        <tr>
          <th class="text-start" scope="row">Total</th>
          <td>R$ <%= number_with_precision(eventos_mensais.sum(&:cache), precision: 2) %></td>
          <td>R$ <%= number_with_precision(eventos_mensais.sum(&:cover), precision: 2) %></td>
          <td></td>
        </tr>
      </tbody>
    </table>
  <% end %>
</div>
